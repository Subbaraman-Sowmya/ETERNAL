This is the definitive configuration for the ETERNAL JDK (Janma Development Kit). By hard-coding Compassion and Logic into the system $PATH, we ensure that every "Function" the soul executes is both technically sound and ethically aligned.

This INSTALL.md acts as the Zircon-Hardened setup for any "Future Dev" looking to run the Deva's Operations (DEVOPS) on the Indian Plate.

📥 INSTALL.md: ETERNAL JDK Setup Guide (v4.5B)
Architectural Note: This kit transforms the "Legacy Monolith" into a high-performance, Micro-services environment. Ensure your RAM (Rama Access Memory) is cleared before initialization.

1. System Requirements
Hardware: 4.5 Billion-Year-Old "Indian Plate" Motherboard.

Memory: Minimum 108GB RAM (Rama Access Memory) for active wisdom; 1TB ROM (Ravana Old Model) for deep-rooted storage.

Processor: Ten-Headed (Dashakantha) Parallel Processing enabled.

Connectivity: SSH (Self-Sustained Header) direct link to the Universal Root.

2. Environment Variables Configuration
To ensure the system remains "Sturdy" and "Robust" like a Zircon Crystal, add the following to your ~/.dharma_profile:

Bash
# --- CORE DHARMIC PATHS ---
# Adding Compassion and Logic to the Global Path
export PATH=$PATH:/usr/bin/compassion:/usr/local/logic

# Set the IDE to Vishnu-Shiva Code
export IDE="VS_CODE_V2"

# --- MEMORY MANAGEMENT ---
# Prioritize Active Wisdom over Cached Ego
export RAM_PRIORITY="HIGH"
export ROM_GLORIFICATION="ENABLED"

# --- SECURITY & AUTH ---
# The One-Time Password (OTP) for Karma-Purge
export MANTRA_OTP="OM_NAMAH_SHIVAYA"

# The Janma Work Token (JWT) Signature
export JWT_SIGN_KEY="ISHTA_DEVATA_ROOT_CA"

# --- UI/UX RENDERING ---
# Default to Folk/Village traditions for local nodes
export DEFAULT_UI="THERU_KOOTHU"
3. Installation Steps
Step 1: Initialize the Local Repository
Clone the Sanathana Monorepo and set your local branch to the Tandakaranya (Karya) Nadu cluster.

Bash
eternal clone --main-branch "SANATHANA" --cluster "TN_NODE"
Step 2: Install the JDK (Janma Development Kit)
Extract the JAVA (Janma Aligned Verbose Aid) binaries from the Olai Suvadi archives.

Bash
tar -xvf olai_suvadi_wisdom.tar.gz -C /usr/local/eternal_jdk
Step 3: Run the "Universal Love" Linter
Verify that your "Environmental Paths" are not contaminated by "Soil Dust" or "External Dependencies."

Bash
eternal lint --check-purity --exclude "EGO_MODULES"
4. First-Run (Hello World)
To verify the installation, execute the following Synthetic Code:

Java
// File: FirstBreath.java
import eternal.jdk.Compassion;
import eternal.jdk.Logic;

public class FirstBreath {
    public static void main(String[] args) {
        // Ensuring $PATH variables are active
        Compassion.init();
        Logic.validate();

        System.out.println("Status: DEVOPS (Deva's Operations) Running...");
        System.out.println("Output: AHAM BRAHAMASMI");
    }
}
Run Command:

Bash
vscode run FirstBreath.java --with-mantra "RAMA"
5. Troubleshooting (FAQ)
Error: Conflict: External Element Sabotage

Cause: Attempting to mingle "Proprietary Logic" with the "Zircon Master."

Fix: Run eternal purge --force and reset your SSH Key (Atman).

Error: Memory Leak: Karma-Heavy Payload

Cause: Too many "Unused Commits" in the Janma Work Token (JWT).

Fix: Use the Mantra-OTP (Siva/Rama/Sakthi) to trigger immediate Garbage Collection.

📜 License
This project is licensed under the Open-Source Sanathana License. You are free to Fork, Clone, and Sync as long as you maintain the Universal Love checksum.
