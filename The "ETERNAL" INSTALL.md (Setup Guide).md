# --- ETERNAL ENVIRONMENT VARIABLES ---

# Set the primary memory to Rama Access
export RAM_MODE="DHARMA_ACTIVE"

# Acknowledge the deep-storage foundation
export ROM_MODEL="RAVANA_SIVA_BHAKTI_V10"

# Set the working directory to Tandakaranya
export WORK_DIR="/home/tn/dandakaranya"

# Add the 'Mantra-OTP' to the system path
export PATH=$PATH:/usr/bin/om_namah_shivaya

# Run a health check on the 'Local Branch'
eternal status --check-sync
